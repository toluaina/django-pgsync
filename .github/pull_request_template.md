## What does this PR do?

<!-- A short description, and a link to the related issue if there is one. -->

## Checklist

- [ ] Tests added/updated (`python tests/runtests.py` passes)
- [ ] Lint passes (`ruff check . && ruff format --check .` — or just commit with pre-commit installed)
- [ ] `CHANGELOG.md` updated under the `(unreleased)` section
- [ ] README updated if behavior or configuration changed
